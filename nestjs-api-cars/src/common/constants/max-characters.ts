export enum MaxCharacter {
  MIN_NAMES = 1,
  MAX_NAMES = 100,
  MIN_USERS = 5,
  MAX_USERS = 50,

  MIN_PASSWORDS = 6,
  // eslint-disable-next-line @typescript-eslint/no-duplicate-enum-values
  MAX_PASSWORDS = 50,
  // eslint-disable-next-line @typescript-eslint/no-duplicate-enum-values
  MIN_DESCRIPTIONS = 1,
  MAX_DESCRIPTIONS = 1500,
  // eslint-disable-next-line @typescript-eslint/no-duplicate-enum-values
  MIN_LINKS = 1,
  MAX_LINKS = 500,
  // eslint-disable-next-line @typescript-eslint/no-duplicate-enum-values
  MIN_GENERALS = 1,
  // eslint-disable-next-line @typescript-eslint/no-duplicate-enum-values
  MAX_GENERALS = 100,
}
